# 个人博客健身数据可视化 — 架构文档

> **版本**: v1.0  
> **创建日期**: 2026-07-12  
> **目标**: 在 Hexo 博客 (wenxixixi.cc) 上开辟 `/fitness/` 页面，提供健身数据的日历、图表、肌肉可视化功能  
> **使用场景**: 手机端为主，在健身房随时查看历史训练记录

---

## 一、项目概览

### 1.1 核心功能

| 功能 | 说明 |
|------|------|
| 📅 月历视图 | 按月显示训练记录，颜色圆点标记训练部位，点击日期弹出详情 |
| 🔥 热力图 | GitHub 贡献图风格，展示训练频率分布 |
| 📈 体重趋势 | 折线图，体重 + 体脂率随时间变化 |
| 📊 训练统计 | 部位频率饼图 + 每周训练天数柱状图 |
| 🦴 肌肉可视化 | SVG 人体正反面轮廓，当天训练到的肌肉区域动态标红 |

### 1.2 设计原则

- **移动端优先**：所有 UI 以手机竖屏为第一目标
- **零后端**：纯静态页面 + JSON 数据，无需 API 服务器
- **数据只读**：绝不修改原始 `fitness_log.md`
- **容错优先**：解析失败不阻断，异常记录跳过 + 日志告警
- **可扩展**：动作库、映射表随训练增长自动进化

---

## 二、系统架构

### 2.1 整体数据流

```
┌─────────────────────────────────────────────────────────────┐
│                        服务器 (216.219.95.228)               │
│                                                             │
│  /root/fitness_log.md          /root/myblog/                │
│  (原始数据·只读)               (Hexo 博客源码)               │
│        │                            │                       │
│        ▼                            ▼                       │
│  ┌──────────────┐           ┌──────────────┐                │
│  │ parse_fitness │  ──────▶ │  /fitness/   │                │
│  │    .py        │  写入     │  静态页面    │                │
│  └──────────────┘           │  HTML+JS+CSS │                │
│        │                    └──────┬───────┘                │
│        ▼                           │                        │
│  ┌──────────────┐                  │  hexo deploy            │
│  │  data.json   │                  ▼                        │
│  └──────┬───────┘           ┌──────────────┐                │
│         │                   │ /var/www/html │               │
│         │  直接写入          │  /fitness/    │               │
│         └──────────────────▶│  data.json    │               │
│                             │  index.html   │               │
│                             │  js/*.js      │               │
│                             │  css/*.css    │               │
│                             └──────────────┘                │
│                                     │                       │
│  ┌──────────────┐                   │                       │
│  │ exercise_map │  (动作→肌肉映射)   │                       │
│  │   .json      │                   │                       │
│  └──────────────┘                   │                       │
│                                     │                       │
│  ┌──────────────┐                   │                       │
│  │ exercise_map │  (待确认映射·缓冲) │                       │
│  │ _pending.json│                   │                       │
│  └──────────────┘                   │                       │
└─────────────────────────────────────┼───────────────────────┘
                                      │
                              ┌───────▼────────┐
                              │  用户浏览器     │
                              │  (手机为主)     │
                              │                │
                              │  fetch JSON    │
                              │  Alpine.js     │
                              │  Chart.js      │
                              │  SVG 肌肉图    │
                              └────────────────┘
```

### 2.2 关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| data.json 落点 | `/var/www/html/fitness/` 直写 | 绕过 Hexo 构建，更新秒生效 |
| 前端框架 | Alpine.js (CDN) | ~15KB，适合 4 个 Tab 的轻量状态管理 |
| 图表库 | Chart.js (CDN) | ~60KB，满足折线/饼/柱三种图表需求 |
| 日历 | 手写 JS (~80行) | 定制需求强，现有组件难满足 |
| 热力图 | 手写 JS (~100行) | 避免引入 cal-heatmap 额外 50KB |
| CSS | 手写 + CSS variables | 深色/浅色跟随系统，零依赖 |
| 构建工具 | 无 | 纯静态文件，CDN 加载库，零配置 |

---

## 三、文件结构

```
/root/myblog/source/fitness/          # Hexo 源码目录
├── index.md                          # Hexo 页面元数据 (layout: page, title: 健身)
│
├── js/
│   ├── app.js                        # 入口：Tab 切换、数据加载、全局状态
│   ├── calendar.js                   # 日历 Tab：月历网格 + 颜色圆点 + 详情弹窗
│   ├── heatmap.js                    # 热力图 Tab：年度/月度训练频率
│   ├── weight-chart.js               # 体重 Tab：Chart.js 折线图
│   ├── stats.js                      # 统计 Tab：饼图 + 柱状图
│   └── muscle-map.js                 # 肌肉高亮引擎：SVG 分区映射 + 高亮控制
│
├── css/
│   └── fitness.css                   # 全部样式，CSS variables 驱动深浅色
│
├── img/
│   └── body-outline.svg              # 人体正反面轮廓 SVG
│       ├── [正面] 胸大肌、三角肌前束、肱二头肌、腹直肌、
│       │         股四头肌、前臂屈肌、胫骨前肌、斜方肌
│       └── [背面] 背阔肌、斜方肌、三角肌后束、肱三头肌、
│                 竖脊肌、臀大肌、腘绳肌、腓肠肌
│
└── data/
    └── exercise_map.json             # 动作→肌肉映射表（人工审核过的正式表）

/root/scripts/
├── parse_fitness.py                  # Markdown → JSON 解析引擎
├── match_exercises.py                # 动作→肌肉自动匹配逻辑
└── README.md                         # 解析器使用说明
```

### 部署目标

```
/var/www/html/fitness/                # Nginx 直接托管
├── index.html                        # Hexo 构建后的页面（Hexo 管）
├── data.json                         # 解析脚本直写（绕过 Hexo）
├── js/                               # Hexo 构建时拷贝
├── css/                              # Hexo 构建时拷贝
└── img/                              # Hexo 构建时拷贝
```

---

## 四、数据模型

### 4.1 data.json 结构

```json
{
  "meta": {
    "generated_at": "2026-07-12T12:00:00+08:00",
    "source_file": "/root/fitness_log.md",
    "data_version": 42,
    "parse_summary": {
      "total_days": 52,
      "parsed_ok": 50,
      "skipped": 2,
      "new_actions_unmapped": 1,
      "errors": []
    }
  },
  "profile": {
    "start_date": "2026-03-05",
    "height_cm": 167,
    "current_weight_kg": 63.35,
    "current_bodyfat_pct": 19.7,
    "goal_weight_kg": 68.0,
    "phase": "增肌期"
  },
  "weight_log": [
    {"date": "2026-05-22", "weight_kg": 62.5, "bodyfat_pct": null},
    {"date": "2026-05-29", "weight_kg": 64.4, "bodyfat_pct": null}
  ],
  "days": [
    {
      "date": "2026-05-22",
      "weekday": "周五",
      "has_training": true,
      "body_parts": ["腿"],
      "exercises": [
        {
          "name": "杠铃深蹲",
          "sets": 5,
          "reps_range": "7-10",
          "weight_kg": 60.0,
          "weight_display": "50×1 + 60×4",
          "muscles": ["股四头肌", "臀大肌", "腘绳肌"],
          "muscle_mapped": true
        },
        {
          "name": "俯卧腿弯举",
          "sets": 5,
          "reps_range": "10",
          "weight_kg": 25.0,
          "weight_display": "25kg",
          "muscles": ["腘绳肌"],
          "muscle_mapped": true
        }
      ],
      "diet": {
        "total_kcal": 1684,
        "protein_g": 85.2,
        "carbs_g": 197,
        "fat_g": 52
      },
      "weight_kg": 62.5,
      "notes": null
    }
  ]
}
```

### 4.2 exercise_map.json 结构

```json
{
  "meta": {
    "last_updated": "2026-07-12",
    "total_entries": 35,
    "auto_mapped": 28,
    "manual_mapped": 7
  },
  "muscles": {
    "杠铃深蹲": {
      "muscles": ["股四头肌", "臀大肌", "腘绳肌", "核心肌群"],
      "source": "manual",
      "added": "2026-07-12"
    },
    "卧推": {
      "muscles": ["胸大肌", "三角肌前束", "肱三头肌"],
      "source": "auto",
      "confidence": 0.95,
      "added": "2026-07-12"
    }
  }
}
```

### 4.3 exercise_map_pending.json 结构

```json
{
  "pending": [
    {
      "name": "分动式拉背训练器",
      "detected_date": "2026-07-12",
      "guessed_muscles": ["背阔肌", "大圆肌"],
      "confidence": 0.65,
      "reason": "关键词匹配'拉背'→背部集群，但无法区分具体器械变体"
    }
  ],
  "resolved": []
}
```

---

## 五、前端架构

### 5.1 页面布局（手机端 375×812 基准）

```
┌──────────────────────────┐
│  ◀ 2026年 7月 ▶          │  ← 月份导航条 (48px)
├──────────────────────────┤
│                          │
│     📅 日历 / 热力图       │
│     📈 体重 / 📊 统计      │  ← Tab 内容区 (自适应高度)
│                          │
│                          │
├──────────────────────────┤
│ [日程] [热度] [体重] [统计]│  ← 底部导航栏 (56px)
└──────────────────────────┘
```

### 5.2 各 Tab 详细设计

#### Tab 1: 📅 日历

```
┌──────────────────────────┐
│  ◀     2026年7月     ▶  │
├──────────────────────────┤
│ 日  一  二  三  四  五  六 │
│              1   2   3  │
│  4   5   6   7   8   9  │     ← 日期格子 40×40px (手机)
│  🔴 11  12  🔵 14  15  │     ← 有训练日显示颜色圆点
│ 16  17  🔴 19  20  🟢  │
│ 22  23  24  🔵 26  27  │     ← 有训练日圆点在日期下方
│ 28  29  30  31          │     ← 今天用蓝色边框高亮
├──────────────────────────┤
│  ▼ 详情卡片 (从底部滑出)  │
│                          │
│  7月8日 周三 · 练背日     │
│  体重 65.8kg · 体脂 —    │
│  ────────────────────    │
│  对握下拉    4组×28次     │
│  坐姿划船    4组×32次     │
│  高位下拉    4组×28次     │
│  分动式高拉  4组×32次     │
│  悬垂举腿    4组×60次     │
│  ────────────────────    │
│  🦴 [查看肌肉图解 ▼]     │
│  🍽️ 饮食: 3227kcal      │
└──────────────────────────┘
```

**颜色规则**：
- 🔴 红色圆点 —— 腿部
- 🔵 蓝色圆点 —— 胸部
- 🟢 绿色圆点 —— 背部
- 🟡 黄色圆点 —— 肩部
- 🟣 紫色圆点 —— 手臂(二头/三头)
- 🟤 棕色圆点 —— 有氧
- ⚪ 灰色 —— 休息日（仅饮食记录）

**多部位日处理**：同一天练多个部位时，显示两个小圆点（左右排列），如 🔴🔵。

**详情卡片**：底部 Sheet 形式弹出，覆盖屏幕 60%，可下滑关闭。展开"肌肉图解"后，详情卡片上方显示人体 SVG，正面/背面分别标红当天练到的所有肌肉。

#### Tab 2: 🔥 热力图

```
┌──────────────────────────┐
│  2026 年训练热力图        │
│                          │
│  一 二 三 四 五 六 日     │
│           ░ ░ ░ ░ ░     │  ← 5月
│  ░ ░ ░ ░ ░ ░ ▒         │
│  ▒ ░ ▓ ░ ░ ▒ ░         │  ← 6月
│  ▓ ▒ ▒ ▓ ░ ░ ░         │
│  ▒ ░ ░ ░ ░ ▒ ░         │
│  ░ ░ ⬜ ⬜ ⬜ ⬜ ⬜       │  ← 7月 (未来日期灰色)
│                          │
│  ░ 休息  ▒ 1-2部位      │
│  ▓ 3-4部位  ⬜ 未来      │  ← 图例
└──────────────────────────┘
```

**自适应范围**：不强制展示全年 365 天。初始化时只展示有数据的范围（5月-至今），随着数据积累自动扩展。

**颜色强度**：按当天训练的不同部位数量计算（0=灰色，1-2=浅绿，3-4=深绿）。

#### Tab 3: 📈 体重趋势

```
┌──────────────────────────┐
│  体重趋势                 │
│                          │
│  66kg ┤    ·             │
│  65kg ┤  ·   ·· ·  ·    │  ← 折线图 (Chart.js)
│  64kg ┤·  · ·    ·      │     蓝色实线 = 体重
│  63kg ┤ ·               │     绿色虚线 = 目标体重 68kg
│  62kg ┤                 │
│       └─┬─┬─┬─┬─┬─┬─    │
│       5月   6月   7月     │
│                          │
│  最新: 63.35kg           │
│  起始: 62.50kg (+0.85)  │  ← 摘要卡片
│  最高: 66.00kg (6/30)   │
└──────────────────────────┘
```

**稀疏数据处理**：不是每天都有体重数据，Chart.js 配置 `spanGaps: false`，让缺失日期自然断开（不连线），视觉上诚实。

#### Tab 4: 📊 统计

```
┌──────────────────────────┐
│  训练部位分布 (饼图)       │
│                          │
│       ◉                  │
│    背 30% 腿 25%         │  ← Chart.js 饼图
│     肩 20% 胸 20%        │
│       手臂 5%            │
│                          │
├──────────────────────────┤
│  每周训练天数 (柱状图)     │
│                          │
│  4天 ┤   ██              │
│  3天 ┤ █████  ██         │  ← Chart.js 柱状图
│  2天 ┤ ███████████       │
│  1天 ┤ ██████████████    │
│      └─┬──┬──┬──┬──┬─    │
│      W1 W2 W3 W4 W5     │
└──────────────────────────┘
```

**饼图优化**：如果部位超过 5 个，合并占比 < 5% 的分组为"其他"。

---

## 六、解析器设计

### 6.1 parse_fitness.py 核心逻辑

```
输入: /root/fitness_log.md
      /root/myblog/source/fitness/data/exercise_map.json
      /root/myblog/source/fitness/data/exercise_map_pending.json

输出: /var/www/html/fitness/data.json  ← 直接写部署目录

处理流程:
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 读取 MD  │───▶│ 分段解析  │───▶│ 提取字段  │───▶│ 动作匹配  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                     │
                                    ┌────────────────┘
                                    ▼
                            ┌──────────────┐
                            │ 查映射表      │
                            │ exercise_map │
                            └──┬───────┬───┘
                               │       │
                            找到     未找到
                               │       │
                               ▼       ▼
                          正常映射  关键词推理
                                     │
                              ┌──────┴──────┐
                              │             │
                          置信度≥0.8   置信度<0.8
                              │             │
                              ▼             ▼
                         写入 pending    写入 pending
                         + 标记高置信     + 低置信告警
                                         + Hermes 待介入
```

### 6.2 解析策略——应对格式不统一

`fitness_log.md` 至少存在 3 种格式变体：

| 时间段 | 格式特征 |
|--------|---------|
| 5月中-5月底 | `### 2026-05-22（周五）` + 表格格式动作 `\| 杠铃深蹲 \| 5组 \| 50×1 + 60×4` |
| 6月初-6月中 | `### 2026-06-07（周日）` + `**💪 今日训练：肩**` + 列表格式 `- 杠铃深蹲 \| ...` |
| 6月底-7月 | `### 2026-07-08（周三）` + `**💪 今日训练：背**` + 列表格式，饮食格式多变 |

**解析策略**：
1. **日期识别**：正则匹配 `### 2026-\d{2}-\d{2}` 和 `### \d{4}-\d{2}-\d{2}` 两种模式
2. **训练部位提取**：按优先级 `**部位：XX**` > `**💪 今日训练：XX**` > 从动作名反向推断
3. **动作表格/列表**：分两套解析器，先试探格式再选择（表格优先，容错退到列表）
4. **饮食表格**：列数动态适应（2列/4列/5列），缺列补 null
5. **失败隔离**：某条记录解析异常 → 跳过 + 记录到 `parse_summary.errors`，其余继续

### 6.3 动作→肌肉自动匹配策略

```python
# 关键词 → 肌肉群组推理规则（示意，非完整代码）

KEYWORD_RULES = {
    "深蹲": ["股四头肌", "臀大肌", "腘绳肌"],
    "卧推|推胸|夹胸": ["胸大肌"],
    "引体|下拉|划船|拉背": ["背阔肌", "大圆肌", "菱形肌"],
    "推肩|飞鸟|面拉|侧平举|前平举|耸肩": ["三角肌"],
    "弯举": ["肱二头肌"],
    "臂屈伸|下压|过头臂屈伸": ["肱三头肌"],
    "腿弯举|腿屈伸": ["股四头肌", "腘绳肌"],
    "挺身": ["竖脊肌", "臀大肌"],
    "卷腹|举腿|健腹轮|腹": ["腹直肌", "腹斜肌"],
    "保加利亚|哈克": ["股四头肌", "臀大肌"],
    "夹腿|扩腿": ["内收肌群", "外展肌群"],
}

# 置信度计算：
# - 主关键词命中 = 0.9+
# - 仅辅助词命中 = 0.5-0.7
# - 完全无匹配 = 0 (需 Hermes 介入)
```

### 6.4 解析校验摘要

每次解析完成后输出：

```
📊 解析摘要
━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 成功解析: 52 天
⚠️ 格式异常跳过: 2 条
🆕 新动作待映射: 1 个 (分动式拉背训练器)
   → 已自动推测写入 pending
📁 输出: /var/www/html/fitness/data.json (156KB)
━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 七、肌肉可视化设计

### 7.1 SVG 肌肉分区

人体轮廓 SVG 分**正面 9 区 + 背面 7 区**，共 16 个可独立着色的 `<path>`：

**正面**：
```
┌─────────────────┐
│    斜方肌(颈侧)  │
│  ┌───────────┐  │
│  │ 三角肌前束 │  │
│  ├───────────┤  │
│  │  胸大肌   │  │
│  ├───────────┤  │
│  │ 肱二头肌  │  │
│  ├───────────┤  │
│  │ 前臂屈肌  │  │
│  ├───────────┤  │
│  │  腹直肌   │  │
│  ├───────────┤  │
│  │ 股四头肌  │  │
│  ├───────────┤  │
│  │ 胫骨前肌  │  │
│  └───────────┘  │
└─────────────────┘
```

**背面**：
```
┌─────────────────┐
│    斜方肌       │
│  ┌───────────┐  │
│  │ 三角肌后束 │  │
│  ├───────────┤  │
│  │  背阔肌   │  │
│  ├───────────┤  │
│  │ 肱三头肌  │  │
│  ├───────────┤  │
│  │  竖脊肌   │  │
│  ├───────────┤  │
│  │  臀大肌   │  │
│  ├───────────┤  │
│  │  腘绳肌   │  │
│  ├───────────┤  │
│  │  腓肠肌   │  │
│  └───────────┘  │
└─────────────────┘
```

### 7.2 高亮逻辑

```
用户点击日期 → 获取当天 exercises 列表
                        │
                        ▼
              收集所有 exercise.muscles
                        │
                        ▼
              去重合并 → 当天涉及的肌肉集合
                        │
                        ▼
              muscle-map.js 遍历 SVG 中所有 <path>
              - 匹配到的 path → fill:#FF4444 (红色) + 透明度过渡动画
              - 未匹配的 path → fill: 浅灰 (跟随深色/浅色主题)
```

### 7.3 移动端展示

肌肉图在详情卡片内，默认收折，点击"🦴 查看肌肉图解 ▼"展开。正反面各占约 150px 宽，手机竖屏并排显示。

---

## 八、深色模式适配

使用 CSS variables 全局控制，跟随 `prefers-color-scheme` 媒体查询：

```css
:root {
  --bg-primary: #FFFFFF;
  --bg-secondary: #F5F5F5;
  --text-primary: #1A1A1A;
  --text-secondary: #666;
  --card-bg: #FFFFFF;
  --border: #E0E0E0;
  --muscle-idle: #DDDDDD;    /* SVG 肌肉默认色 */
  --muscle-active: #FF4444;  /* SVG 肌肉高亮色 */
  --accent: #3B82F6;
}

@media (prefers-color-scheme: dark) {
  :root {
    --bg-primary: #1A1A1A;
    --bg-secondary: #252525;
    --text-primary: #EEEEEE;
    --text-secondary: #AAAAAA;
    --card-bg: #2A2A2A;
    --border: #3A3A3A;
    --muscle-idle: #3A3A3A;
    --muscle-active: #FF6666;
    --accent: #60A5FA;
  }
}
```

Chart.js 图表也需要在系统主题切换时重建实例（用 `window.matchMedia` 监听 + 重绘）。

---

## 九、与 Hexo/Butterfly 的集成

### 9.1 导航菜单

在 `_config.butterfly.yml` 中：

```yaml
menu:
  首页: / || fas fa-home
  健身: /fitness/ || fas fa-dumbbell
  归档: /archives/ || fas fa-archive
```

### 9.2 CSS/JS 注入

在 `_config.butterfly.yml` 的 `inject` 部分注入（或者在 `index.md` 的 front matter 中引入）：

```yaml
inject:
  head:
    - <link rel="stylesheet" href="/fitness/css/fitness.css">
    - <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
    - <script src="https://cdn.jsdelivr.net/npm/chart.js@4.x.x/dist/chart.umd.min.js"></script>
  bottom:
    - <script src="/fitness/js/muscle-map.js"></script>
    - <script src="/fitness/js/calendar.js"></script>
    - <script src="/fitness/js/heatmap.js"></script>
    - <script src="/fitness/js/weight-chart.js"></script>
    - <script src="/fitness/js/stats.js"></script>
    - <script src="/fitness/js/app.js"></script>
```

### 9.3 PJAX 兼容

Butterfly 主题可能开启 PJAX（页面无刷新加载）。所有 JS 需要在 `pjax:complete` 事件后重新初始化：

```javascript
document.addEventListener('pjax:complete', () => {
  if (window.location.pathname.startsWith('/fitness/')) {
    initApp();
  }
});
```

### 9.4 Hexo 页面入口

`source/fitness/index.md`：

```markdown
---
title: 健身
date: 2026-07-12
layout: page
comments: false
---

<div id="fitness-app" x-data="fitnessApp()" x-init="init()">
  <!-- 页面结构由 JS 动态渲染 -->
</div>
```

---

## 十、实施阶段

| 阶段 | 内容 | 预估工时 | 依赖 |
|------|------|---------|------|
| **P0** | 解析脚本 + 初始映射表 + data.json 生成 | 1 天 | fitness_log.md + 35 个动作映射 |
| **P1** | 日历 Tab + 详情卡片 + 肌肉 SVG | 2 天 | P0 输出的 data.json |
| **P2** | 体重折线图 + 统计图表 + 热力图 | 1 天 | P0 |
| **P3** | 深色模式 + 移动端调优 + Butterfly 集成 + 真机测试 | 1 天 | P1+P2 |

---

## 十一、风险与缓解

| 风险 | 严重度 | 缓解措施 |
|------|--------|---------|
| 肌肉 SVG 绘制工时超预期 | 高 | P1 阶段先找开源解剖图 SVG 改，或降级为文字列表 |
| fitness_log.md 格式持续变化 | 中 | 解析器做版本化策略，加测试用例覆盖已知格式 |
| 手机日历格子触控太小 | 高 | 最小触控区域 44×44px (iOS HIG)，格子不够大就加 padding |
| data.json 体积膨胀 | 中 | 按需加载：首屏只包含当月 + 摘要，其余懒加载 |
| Chart.js 深色模式自适应 | 低 | `window.matchMedia` 监听，重建实例 |
| Butterfly PJAX 冲突 | 中 | PJAX 事件重新绑定，加降级方案 |

---

## 十二、初始动作→肌肉映射表

以下是 `fitness_log.md` 中出现的全部动作及其肌肉映射（**需在实现时审核准确性**）：

| 序号 | 动作 | 部位分类 | 主要肌肉 |
|------|------|---------|---------|
| 1 | 杠铃深蹲 | 腿 | 股四头肌、臀大肌、腘绳肌 |
| 2 | 哈克深蹲 | 腿 | 股四头肌、臀大肌 |
| 3 | 保加利亚蹲 | 腿 | 股四头肌、臀大肌 |
| 4 | 俯卧腿弯举 | 腿 | 腘绳肌 |
| 5 | 山羊挺身 | 腿/背 | 竖脊肌、臀大肌、腘绳肌 |
| 6 | 夹腿机夹腿 | 腿 | 内收肌群 |
| 7 | 夹腿机扩腿 | 腿 | 外展肌群 |
| 8 | 卧推 | 胸 | 胸大肌、三角肌前束、肱三头肌 |
| 9 | 上斜哑铃推胸 | 胸 | 胸大肌上部、三角肌前束 |
| 10 | 坐姿推胸训练器 | 胸 | 胸大肌 |
| 11 | 悍马坐姿推胸 | 胸 | 胸大肌 |
| 12 | 分动式上斜器械推胸 | 胸 | 胸大肌上部、三角肌前束 |
| 13 | 坐姿平推 | 胸 | 胸大肌 |
| 14 | 双杠臂屈伸 | 胸+三头 | 胸大肌下部、肱三头肌 |
| 15 | 蝴蝶机夹胸 | 胸 | 胸大肌中缝 |
| 16 | 龙门架绳索夹胸 | 胸 | 胸大肌中缝 |
| 17 | 坐姿下斜推胸 | 胸 | 胸大肌下部 |
| 18 | 史密斯推肩 | 肩 | 三角肌前束、中束 |
| 19 | 哑铃推肩 | 肩 | 三角肌前束、中束 |
| 20 | 飞鸟 | 肩 | 三角肌中束 |
| 21 | 绳索面拉 | 肩 | 三角肌后束、菱形肌 |
| 22 | 绳索前平举 | 肩 | 三角肌前束 |
| 23 | 绳索侧平举 | 肩 | 三角肌中束 |
| 24 | 哑铃耸肩 | 肩 | 斜方肌 |
| 25 | 固定器械飞鸟 | 肩 | 三角肌中束 |
| 26 | 高位下拉 | 背 | 背阔肌、大圆肌 |
| 27 | 对握下拉 | 背 | 背阔肌 |
| 28 | 悍马坐姿高位下拉 | 背 | 背阔肌 |
| 29 | 分动式高拉背 | 背 | 背阔肌 |
| 30 | 划船（铁拉手/塑料拉手/铁把手） | 背 | 背阔肌、菱形肌、斜方肌中下部 |
| 31 | 悍马坐姿划船 | 背 | 背阔肌 |
| 32 | 分动式拉背训练器 | 背 | 背阔肌、大圆肌 |
| 33 | 单臂下拉 | 背 | 背阔肌 |
| 34 | 自重引体向上 | 背 | 背阔肌、肱二头肌 |
| 35 | 坐姿单臂哑铃弯举 | 手臂 | 肱二头肌 |
| 36 | 坐姿下压推三头 | 手臂 | 肱三头肌 |
| 37 | 绳索过头臂屈伸 | 手臂 | 肱三头肌 |
| 38 | 坐姿哑铃颈后臂屈伸 | 手臂 | 肱三头肌 |
| 39 | 仰卧杠铃臂屈伸 | 手臂 | 肱三头肌 |
| 40 | 悬垂举腿 | 腹 | 腹直肌、腹斜肌 |
| 41 | 跪姿卷腹 | 腹 | 腹直肌 |
| 42 | 健腹轮 | 腹 | 腹直肌 |
| 43 | 滚腹轮卷腹 | 腹 | 腹直肌 |
| 44 | 悬吊卷腹 | 腹 | 腹直肌 |

---

## 十三、后续扩展（不做但预留）

- **Hermes Skill**：自动检测 pending 映射，查询补充后自动更新映射表
- **小米健康数据接入**：解析 `mi_fitness` SDK 输出的心率/睡眠/步数
- **训练计划功能**：预设下周训练安排，自动填充日历
- **PR 追踪**：记录每个动作的最大重量，自动高亮新 PR

---

> **此架构文档供 Claude Code 实现使用。所有文件路径、字段名、接口定义已明确定义，无需再猜测。**
